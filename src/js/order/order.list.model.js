(function ($, window) {
	IX.ns("Hualala.Order");
	var popoverMsg = Hualala.UI.PopoverMsgTip;
	var toptip = Hualala.UI.TopTip;

	var OrderListModel = Stapes.subclass({
		constructor : function (cfg) {
			
		}
	});

	Hualala.Order.OrderListModel = OrderListModel;
})(jQuery, window);