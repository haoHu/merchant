(function ($, window) {
	IX.ns("Hualala.Order");
	var popoverMsg = Hualala.UI.PopoverMsgTip;
	var toptip = Hualala.UI.TopTip;

	var OrderListView = Stapes.subclass({
		constructor : function (cfg) {
			
		}
	});

	Hualala.Order.OrderListView = OrderListView;
})(jQuery, window);